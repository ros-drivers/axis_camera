<launch>
  <!-- This launchfile should bring up a node that broadcasts a ros image
       transport on /webcam/image_raw -->

  <arg name="DEVICE" default="http://localhost:3080/mjpg/video.mjpg"/>
  <!-- The GStreamer framerate needs to be an integral fraction -->
  <arg name="FPS" default="10/1"/>
  <arg name="PUBLISH_FRAME" default="false"/>

  <node ns="v4l" name="gscam_driver_v4l" pkg="gscam" type="gscam" output="screen">
    <param name="camera_name" value="default"/>
    <param name="camera_info_url" value="package://gscam/examples/uncalibrated_parameters.ini"/>
    <param name="gscam_config" value="souphttpsrc location=$(arg DEVICE) ! multipartdemux ! jpegdec ! video/x-raw-yuv,width=704,height=480 ! ffmpegcolorspace ! video/x-raw-rgb"/>
    <!-- v4l2src device=$(arg DEVICE) ! video/x-raw-rgb,framerate=$(arg FPS) ! ffmpegcolorspace" -->
    <param name="frame_id" value="/gsaxis"/>
    <param name="sync_sink" value="true"/>
  </node>

</launch>
